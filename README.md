# General-Reporting-tool
A web app tool that helps users to get reports as charts, graphs, grids... , Where the user can select the db that he wishes to generate the reports from depending on the server the user enters, also this tool can be used with multiple server engines (Oracle pl/sql, sql server...), developed using .Net framework 4,7,1 and angular 18
